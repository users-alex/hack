DATE=$(date +%Y%m.%d.%s)
# default comment
TAG_VERSION=$(git describe origin/master)

COMMENT="${DATE} : ${TAG_VERSION}"

[ ! -z $1 ] && COMMENT="$1"

git commit -m $COMMENT
echo "last tag : $TAG_VERSION"
